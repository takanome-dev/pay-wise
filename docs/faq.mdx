---
title: FAQ
description: Frequently Asked Questions
---

## What is the difference between a `Component` and a `Container`?

A `Component` is a React component that is connected to the Redux store. A `Container` is a React component that is connected to the Redux store and is also connected to a `Component`. A `Container` is a `Component` that is connected to the Redux store.
